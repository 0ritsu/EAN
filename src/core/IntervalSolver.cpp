#include "IntervalSolver.h"

IntervalResult IntervalSolver::solve(const std::string &rawInput, int maxIter, int mode) {
    (void)rawInput;
    (void)maxIter;
    (void)mode;
    IntervalResult result;
    result.ok = false;
    result.error = "TODO: implementacja solvera przedziałowego";
    return result;
}
